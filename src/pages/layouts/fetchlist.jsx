import React from "react";
import {
    Card,
    CardContent,
    Typography
} from '@mui/material';

const Fetchlist = () => {
    return (
        <div className="fetchlist">
            <Card className="card">
                <CardContent>
                    <Typography variant="h6">Título de la Página FETCHLIST</Typography>
                </CardContent>
            </Card>
        </div>
    );
};

export default Fetchlist;
